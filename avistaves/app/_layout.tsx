import { Stack } from 'expo-router';

/**
 * Layout raíz: navegación tipo pila nativa (RF-06).
 * El header del Stack provee el botón «atrás» en las pantallas apiladas.
 */
export default function LayoutRaiz() {
  return <Stack />;
}