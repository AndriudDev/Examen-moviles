# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Context from /docs

Before implementing any feature or function, read the relevant files in `docs/`:

- `roadmap.md` — estado del proyecto y pendientes (leer siempre primero)
- `brief.md` — requisitos (RFs) e intención del producto
- `stack.md` — arquitectura y decisiones técnicas
- `style.md` — guía de estilo visual (obligatoria para cualquier trabajo de UI)

After finishing a task, update `docs/roadmap.md` (mark completed items), `README.md` if the project structure, commands, or status changed, and any other affected doc so the documentation never drifts from the code.